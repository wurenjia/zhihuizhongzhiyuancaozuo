<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myone01">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_soft_tb"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_soft_tb"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".RegistActivity" />
        <activity android:name="com.example.ABCqu.AquActivity" />
        <activity android:name=".UnrememberPswActivity" />
        <activity android:name="com.example.ABCqu.BquActivity" />
        <activity android:name="com.example.ABCqu.CquActivity" />
        <activity android:name=".ControlActivity" />
        <activity android:name=".ChangePswActivity" />
        <activity android:name=".TransferActivity"></activity>
    </application>

</manifest>